BEGIN {
	print "/* This include file is generated by a bogus awk script... */"
	print "/* Try regenerating it with 'awk -f astmeta.awk nodedef.def' */"
	print ""
	print "#ifndef astmeta_h"
	print "#define astmeta_h"
	print ""
        count = 0
      }
/struct/{
	next
	}
/"/{
        name = substr($1,2,length($1)-2)
	prefix = name
        printf ("#define GEN_%s %d\n", prefix, count)
	count++
	next
      }
/\/\*/{
	next
      }
/{/{
       next
      }
/}/{
       next
      }
END   {
        printf ("#define GEN_META_LAST %d\n", count)
	print ""
	print "#endif"
      }
